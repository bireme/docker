services:
  uptime:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3001"
    volumes:
      - ${APP_DATA_PATH}:/app/data
    networks:
      - nginx-proxy

networks:
  nginx-proxy:
    external: true
