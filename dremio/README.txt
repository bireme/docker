# Notas gerais

- O servidor host onde será executado o master/executor deve ter no /etc/hosts uma entrada que resolva o proprio nome do server

Exemplo

/etc/hostname
dockerdata01

/etc/hosts
127.0.0.1 localhost
127.0.0.1 dockerdata01